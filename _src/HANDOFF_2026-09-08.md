**Date:** 2026-09-08
**Status:** CURRENT

# First session in this repo: run these, in this order

The clone is 7 commits behind `origin/main` and its HEAD (`2346759`) is a direct ancestor, so
the pull fast-forwards. Nothing has diverged.

Two files in the working tree are in the way, and the session that prepared this could not
delete files (the Cowork device bridge blocks unlink). Both are build output and fully
reproducible, so removing them loses nothing:

    cd ~/Documents/kodamatrade_site
    rm -f strategy.html .git/index.lock.stale_2026-09-08
    git checkout -- subscriptions.html      # no-op if already clean
    git pull --ff-only

Now prove the build reproduces exactly what is deployed. This is the real acceptance test:

    git stash list                          # expect empty
    shasum -a 256 strategy.html subscriptions.html our-story.html > /tmp/before.txt
    python3 _src/build.py
    shasum -a 256 -c /tmp/before.txt        # expect three OK

Three OK means `_src/` reproduces the live site byte for byte and you can trust the build.
If any line fails, STOP and work out why before committing anything.

Then commit the build system itself, which is not yet in git:

    git add -A
    git commit -m "Add build sources, builder and CLAUDE.md"
    git push

Expected sizes, as deployed and verified live on 2026-09-08:

    strategy.html       202,748 bytes
    subscriptions.html  187,010 bytes
    our-story.html      173,808 bytes
    index.html          168,575 bytes   (hand-edited, not built)
    members.html        162,822 bytes   (hand-edited, not built)

Read `CLAUDE.md` at the repo root before editing any page. It carries the build assertions and
why each exists, where performance figures come from, the alerts-not-signals rule, and the
compliance status of these pages.
